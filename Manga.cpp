#include "Manga.h"

Manga::Manga(string userTitle, string userAuthor, string userPublisher, int userQuantity, int userPrice) {
    title = userTitle;
    author = userAuthor;
    publisher = userPublisher;
    quantity = userQuantity;
    price = userPrice;
}

